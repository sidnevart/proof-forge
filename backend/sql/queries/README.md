# SQL queries

Здесь будут лежать SQL-файлы для `sqlc`, когда начнётся реализация feature slices.

На этапе bootstrap каталог существует намеренно:
- чтобы зафиксировать opinionated foundation `pgx + sqlc + goose`
- чтобы не смешивать runtime bootstrap с доменными запросами до начала feature work
